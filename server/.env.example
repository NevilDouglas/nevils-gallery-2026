# Poort waarop de backend draait
PORT=4000

# PostgreSQL databaseconfiguratie
DB_NAME=db_nevils_gallery
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_HOST=localhost

# JWT-secret voor login/authenticatie
JWT_SECRET=replace_with_a_long_random_secret